# nalindard.github.io

This repository hosts a GitHub Pages redirect from
`https://nalindard.github.io/` to `https://nalinda.dev/`.

The site is intentionally minimal and keeps `.nojekyll` to ensure GitHub Pages
serves the static files as-is.
